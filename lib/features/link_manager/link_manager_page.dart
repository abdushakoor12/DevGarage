import 'package:flutter/material.dart';

class LinkManagerPage extends StatelessWidget {
  const LinkManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}